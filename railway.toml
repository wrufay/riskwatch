[build]
builder = "NIXPACKS"
installCommand = "pip install flask flask-cors joblib numpy pandas scipy scikit-learn gunicorn"
watchPatterns = ["api/**", "model/*.pkl", "data/*.csv"]

[deploy]
startCommand = "cd api && python -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
